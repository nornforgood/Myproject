[project]
name = "myproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nornforgood", email = "norn_chang@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.62.0",
]

[project.scripts]
myproject = "myproject:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
